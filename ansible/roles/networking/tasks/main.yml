---
- name: Configure networking (Linux)
  ansible.builtin.include_tasks: linux.yml
  when: ansible_os_family != 'FreeBSD'

- name: Configure networking (FreeBSD)
  ansible.builtin.include_tasks: freebsd.yml
  when: ansible_os_family == 'FreeBSD'
